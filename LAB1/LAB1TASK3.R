# Retrieve radius from user
radius <- as.numeric(readline(prompt = "Enter the radius of the circle: "))

# Calculate the area of the circle
area <- pi * radius^2

# Display the area
print(area)
